# Benchmark run — **sxmc v0.1.6** (crates.io)

## When & how

- **Date:** 2026-03-20 (America/New_York timestamps in raw logs)
- **Binary:** `cargo install sxmc --force` (or `target/release/sxmc` from this repo) → **sxmc 0.1.6**
- **Host:** Linux x86_64, **5 runs** per step, **median** wall time (ms)
- **Reproduce:** from the repo root:

```bash
scripts/benchmark_cli.sh /tmp/benchmark-results.md
# or: SXMC=target/release/sxmc scripts/benchmark_cli.sh
```

The script uses only **`tests/fixtures`** (no dependency on `~/.claude/skills`).

**v0.1.6** adds **`scripts/benchmark_startup.py`** and **`scripts/startup_smoke.sh`** for startup-focused checks; this document remains the **full CLI path** benchmark from **`scripts/benchmark_cli.sh`**.

## Automated test suite (`cargo test`)

At **v0.1.6** tag / `master` at benchmark time:

| Suite | Count |
|-------|------:|
| Library unit tests | **66** |
| `src/main.rs` (unit) | **5** |
| `tests/cli_integration.rs` | **32** |
| Doc tests | **1** |
| **Total** | **104** |

All passed (`0` failed).

## Wall-clock results (median ms)

Generated by **`scripts/benchmark_cli.sh`** (this run: **2026-03-20T18:11:54-04:00**, `SXMC=target/release/sxmc`):

| Scenario | Step | Median (ms) |
|----------|------|-------------:|
| **A** | `sxmc stdio "sxmc serve --paths tests/fixtures" skill_with_scripts__hello` | **11** |
| **B** | `sxmc api <petstore openapi> --list` | **613** |
| **B** | `sxmc api … findPetsByStatus status=available` | **1227** |
| **B** | `curl` only to same Petstore endpoint | **409** |
| **C** | `sxmc stdio "sxmc serve --paths tests/fixtures" --list` | **10** |
| **D** | `sxmc scan --paths tests/fixtures --skill malicious-skill` | **11** |
| **Micro** | Local OpenAPI file + ephemeral `127.0.0.1` server + `sxmc api … listPets` | **14** |

**Interpretation:** Petstore steps are **network-dominated**. **A / C / D / Micro** stay in the **~10–15 ms** range on this machine.

## Historical comparison (approximate)

| Version | A (stdio → fixture script) | B `api --list` (Petstore) |
|---------|----------------------------|---------------------------|
| **0.1.5** (prior doc) | **10** | **614** |
| **0.1.6** (this doc) | **11** | **613** |

Treat **B** deltas as **noise** unless measured with a local mock or many more runs.

## Related validation (v0.1.6 release)

- **Startup / help smoke:** CI runs `sxmc --version` and `sxmc --help` on every OS before tests; see **`scripts/startup_smoke.sh`** and **`scripts/benchmark_startup.py`**.
- **Real skills + npm MCPs + promptless “dialog” checks:** [REAL_WORLD_SKILLS_AND_MCP_REPORT.md](REAL_WORLD_SKILLS_AND_MCP_REPORT.md).

## Operational notes (benchmark maintenance)

| Issue | Resolution |
|-------|------------|
| **`origin/main` missing** | Default branch is **`master`**. |
| **`sxmc scan` exits non-zero** when findings exist | Timing helpers must **not** treat that as a failed timing sample |
| **`git push` HTTP 403** with a restricted PAT | **`unset GITHUB_TOKEN`** then **`gh auth setup-git`** |

The **benchmark script** and **`cargo test`** completed with **exit code 0**; **104/104** tests passed.

## Related

- [VALUE_AND_BENCHMARK_FINDINGS.md](VALUE_AND_BENCHMARK_FINDINGS.md) — methodology & token guidance
- [BENCHMARK_RUN_v0.1.5.md](BENCHMARK_RUN_v0.1.5.md) — prior captured run
- [REAL_WORLD_SKILLS_AND_MCP_REPORT.md](REAL_WORLD_SKILLS_AND_MCP_REPORT.md) — integration + multi-turn notes
